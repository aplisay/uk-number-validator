{
  "name": "uk-number-validator",
  "version": "1.0.0",
  "private": true,
  "description": "Authoritative UK number validator driven by Ofcom weekly CSVs (S1/S3/S5/S7/S8/S9/S10).",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "clean": "rm -rf dist build prefixes.json && mkdir -p dist build data",
    "download": "tsc src/download.ts --outDir dist --skipLibCheck && node dist/download.js",
    "download:cache": "tsc src/download.ts --outDir dist --skipLibCheck && node dist/download.js --no-fetch",
    "build": "tsc -p tsconfig.json",
    "build:all": "yarn clean && yarn download && yarn build",
    "build:all:cache": "yarn clean && yarn download:cache && yarn build",
    "start": "node dist/server.js",
    "dev": "yarn build && yarn start",
    "test": "node dist/test/run-tests.js",
    "bundle": "tar -czf build/uk-number-validator.tar.gz --exclude='./node_modules' --exclude='./build' --exclude='./.git' ."
  },
  "dependencies": {
    "csv-parse": "^5.5.6",
    "node-fetch": "^3.3.2",
    "express": "^4.21.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.11",
    "@types/node-fetch": "^2.6.11",
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "typescript": "^5.6.3"
  }
}